{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for product feedback app to communicate with database",
  "main": "server.js",
  "scripts": {
    "build": "cd ../frontend && npm run build",
    "start:server": "nodemon servr.js",
    "dev": "concurrently \"npm run build -- --watch\" \"npm run start:server\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mysql2": "^3.11.4",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7"
  }
}
